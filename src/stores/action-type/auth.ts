import { SLICE_NAME } from 'stores/types';

export const LOGIN = SLICE_NAME.AUTH + '/login';
export const LOGOUT = SLICE_NAME.AUTH + '/logout';
export const CLEAR_STORE = SLICE_NAME.AUTH + '/clearStore';
