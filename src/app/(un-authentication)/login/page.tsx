// PROJECT IMPORTS
import Login from 'screens/un-authentication/login';

// ================================|| LOGIN ||================================ //

const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
